<!-- jQuery -->
{% assign jquery = site.third_party_libraries.jquery %}
{% assign jquery_url = jquery.url.js | replace: '{{version}}', jquery.version %}
<script
  src="{{ jquery_url }}"
  integrity="{{ jquery.integrity.js }}"
  crossorigin="anonymous"
></script>
